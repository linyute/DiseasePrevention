<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="DiseasePrevention.UserControls.LoadingIndicator">
  <ContentView.Content>

    <StackLayout Orientation="Vertical" Padding="20">
      <ActivityIndicator IsRunning="{Binding IsRunning}" Margin="10" />
      <Label Text="資料讀取中" HorizontalTextAlignment="Center" />
    </StackLayout>

  </ContentView.Content>
</ContentView>
